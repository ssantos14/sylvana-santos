import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

ReactDOM.render(
    <App/>,
    // this corresponds to the id of the div in the index.html file
    document.getElementById('app')
);